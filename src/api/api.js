import Login from "@/api/login";
import examCard from "@/api/examcard";
import questionManager from "@/api/questionManage";

const api = {
  Login,
  examCard,
  questionManager,
};
export default api;
